<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>TrackMoney - Monthly Report</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');
        body { font-family: 'Inter', sans-serif; color: #374151; margin: 0; padding: 0; background-color: #f9fafb; }
        .page { padding: 40px; }
        .brand-color { color: #4f46e5; }
        
        .header { display: flex; justify-content: space-between; align-items: flex-start; border-bottom: 1px solid #e5e7eb; padding-bottom: 20px; }
        .header h1 { margin: 0; color: #4f46e5; font-size: 24px; font-weight: 700; }
        .header-details { text-align: right; }
        .header-details p { margin: 0; font-size: 14px; color: #6b7280; }
        
        .user-info { margin-top: 20px; margin-bottom: 30px; }
        .user-info p { margin: 4px 0; font-size: 14px; }
        .user-info span { font-weight: 600; color: #1f2937; }
        
        .summary-grid { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 20px; margin-bottom: 30px; }
        .card { background-color: #ffffff; border: 1px solid #e5e7eb; border-radius: 12px; padding: 20px; }
        .card h3 { margin: 0 0 8px; font-size: 14px; color: #6b7280; font-weight: 500; }
        .card p { margin: 0; font-size: 28px; font-weight: 700; }
        .income { color: #10b981; } .spending { color: #ef4444; } .balance-positive { color: #2563eb; } .balance-negative { color: #ef4444; }

        .section-title { font-size: 20px; font-weight: 600; color: #111827; margin-bottom: 20px; }
        .content-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 30px; }
        .metric { display: flex; justify-content: space-between; align-items: center; font-size: 14px; padding: 12px 0; border-bottom: 1px solid #f3f4f6; }
        .metric:last-child { border-bottom: none; }
        .metric-label { color: #6b7280; }
        .metric-value { font-weight: 600; color: #1f2937; }
        
        .status-card { background-color: <%= financialStatus.color %>; color: white; border-radius: 12px; padding: 20px; }
        .status-card h3 { margin: 0 0 8px; font-size: 16px; font-weight: 600; opacity: 0.8; }
        .status-card p { margin: 0; font-size: 14px; }
        
        table { width: 100%; border-collapse: collapse; }
        th, td { padding: 12px 15px; text-align: left; font-size: 14px; }
        thead tr { background-color: #f3f4f6; border-bottom: 2px solid #e5e7eb; }
        th { font-weight: 600; color: #4b5563; }
        tbody tr { border-bottom: 1px solid #f3f4f6; }
        .bar { height: 6px; background-color: #4f46e5; border-radius: 3px; }

        .footer { text-align: center; margin-top: 40px; padding-top: 20px; border-top: 1px solid #e5e7eb; font-size: 12px; color: #9ca3af; }
    </style>
</head>
<body>
    <div class="page">
        <div class="header">
            <h1 class="brand-color">TrackMoney</h1>
            <div class="header-details">
                <p style="font-weight: 600;">Monthly Financial Report</p>
                <p><%= monthName %> <%= year %></p>
            </div>
        </div>

        <div class="user-info">
            <p>Report for: <span><%= currentUser.name %></span></p>
            <p>Email: <span><%= currentUser.email %></span></p>
        </div>

        <div class="summary-grid">
            <div class="card">
                <h3>Income</h3>
                <p class="income"><%= summary.totalIncome.toLocaleString('en-IN', { style: 'currency', currency: 'INR' }) %></p>
            </div>
            <div class="card">
                <h3>Spent</h3>
                <p class="spending"><%= summary.totalSpending.toLocaleString('en-IN', { style: 'currency', currency: 'INR' }) %></p>
            </div>
            <div class="card">
                <h3>Balance</h3>
                <p class="<%= summary.remainingBalance >= 0 ? 'balance-positive' : 'balance-negative' %>">
                    <%= summary.remainingBalance.toLocaleString('en-IN', { style: 'currency', currency: 'INR' }) %>
                </p>
            </div>
        </div>
        
        <div class="content-grid">
            <div class="card">
                <h3 class="section-title" style="font-size: 18px; border: none; margin-bottom: 10px;">Key Metrics</h3>
                <div class="metric">
                    <span class="metric-label">Top Spending Category</span>
                    <span class="metric-value"><%= topCategory %></span>
                </div>
                <div class="metric">
                    <span class="metric-label">Highest Spending Day</span>
                    <span class="metric-value">Day <%= highestSpendingDay.day %> (<%= highestSpendingDay.spending.toLocaleString('en-IN', { style: 'currency', currency: 'INR' }) %>)</span>
                </div>
                <div class="metric">
                    <span class="metric-label">Average Daily Spend</span>
                    <span class="metric-value"><%= averageDailySpend.toLocaleString('en-IN', { style: 'currency', currency: 'INR' }) %></span>
                </div>
                <div class="metric">
                    <span class="metric-label">Total Transactions</span>
                    <span class="metric-value"><%= transactionCount %></span>
                </div>
                <div class="metric">
                    <span class="metric-label">Income Burn Rate</span>
                    <span class="metric-value"><%= burnRate %>%</span>
                </div>
            </div>
            <div class="status-card">
                <h3>Financial Status: <%= financialStatus.text %></h3>
                <p><%= financialStatus.tip %></p>
            </div>
        </div>

        <div class="card" style="margin-top: 30px;">
            <h3 class="section-title" style="font-size: 18px; border: none; margin-bottom: 20px;">Spending Breakdown by Category</h3>
            <table>
                <thead><tr><th>Category</th><th>Amount</th><th>Percentage</th></tr></thead>
                <tbody>
                    <% spendingByCategory.forEach(function(item) { %>
                        <tr>
                            <td><%= item.name %></td>
                            <td><%= item.value.toLocaleString('en-IN', { style: 'currency', currency: 'INR' }) %></td>
                            <td>
                                <div style="display: flex; align-items: center; gap: 10px;">
                                    <div style="width: 100px; background-color: #e5e7eb; border-radius: 3px;">
                                        <div class="bar" style="width: <%= (item.value / summary.totalSpending * 100).toFixed(0) %>%;"></div>
                                    </div>
                                    <span><%= (item.value / summary.totalSpending * 100).toFixed(0) %>%</span>
                                </div>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>

        <div class="footer">
            Report generated by TrackMoney on <%= new Date().toLocaleString('en-GB') %>
        </div>
    </div>
</body>
</html>